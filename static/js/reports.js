async function loadReports(){let e=document.getElementById('reports');if(!e)return;let d=await FA.api('/api/reports');e.innerHTML=d.reports?.map(r=>`<div class="row"><div><b>${FA.escape(r.public_id)} · ${FA.escape(r.category)}</b><span>${FA.escape(r.description.slice(0,150))}</span></div><span class="tag">${FA.escape(r.status)}</span></div>`).join('')||'<div class="row">No hay reportes todavía.</div>'}
document.getElementById('reportForm')?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);if(window.__loc){fd.append('latitude',window.__loc.lat);fd.append('longitude',window.__loc.lon)}const r=await FA.upload('/api/reports',fd);FA.toast(r.message,r.ok?'ok':'err');if(r.ok)setTimeout(()=>location.href='/reports',700)});document.getElementById('locBtn')?.addEventListener('click',()=>navigator.geolocation.getCurrentPosition(p=>{window.__loc={lat:p.coords.latitude,lon:p.coords.longitude};document.getElementById('locStatus').textContent='Ubicación capturada temporalmente.'},()=>FA.toast('No pudimos obtener tu ubicación.','err')));loadReports();
